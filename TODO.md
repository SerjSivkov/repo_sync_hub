# TODO / идеи по улучшению

Список составлен по анализу текущего кода (`lib/`, `scripts/`, `test/`).
Приоритеты: **P1** — заметная польза/риск, **P2** — желательно, **P3** — по возможности.

## Производительность сканирования

- [x] **P1. Параллельное сканирование.** Сейчас репозитории опрашиваются строго
  последовательно (`GitScanner.scan`, цикл по `refs`), а внутри каждого — цепочка
  git-команд. На десятках репозиториев это медленно. Добавить пул с ограничением
  параллелизма (например 4–8) и настройку конкуррентности.
- [ ] **P1. `fetch` при скане — по желанию.** `GitScanner.inspectRepo` делает
  `git fetch --all --prune` для каждого репозитория (сетевая операция) при
  `fetchBeforePull`. Из-за этого первый скан долгий. Разделить «быстрый скан»
  (локальные метрики) и «проверку обновлений» (fetch) — вынести fetch в отдельную
  команду/фоновую задачу.
- [ ] **P2. Быстрый расчёт размера.** `GitRunner.repoSizeBytes` вызывает `du -sk`
  по всей рабочей копии (включает `build/`, `node_modules/`). Рассмотреть
  `git count-objects -vH` (размер `.git`) и/или показывать размер `.git` и рабочей
  копии отдельно; кэшировать и не пересчитывать без изменений на диске.
- Показывать только обновления
- Если свернул группу, то сворачивание не должно спадать
- Значок + для клонирования репозитория для каждой группы
- В шапке и в приложениях название repo_sync_hub изменить на RepoSyncHub
- Убрать сверху шапку. Кнопки свернуть закрыть должны быть в самом окне приложения
- В настройках добавить пункт запускать при старте системы
- В настройках добавить пункт сворачивать в статус бар при закрытии приложения
- Запускать сканирование с периодичностью
- Отображение дубликатов по origin ссылке

## Кроссплатформенность

- [ ] **P1. Обобщить действия с ОС.** `RepoShortcut` завязан на macOS
  (`open`, `open -a Terminal`, `~/Desktop`, симлинк-«ярлык»). Требование «любая
  система-приёмник» относится к git, но UI/действия — только macOS. Либо
  абстрагировать под Linux/Windows, либо явно задокументировать ограничение
  и прятать эти пункты меню вне macOS.
- [ ] **P3.** Проверить поведение путей с пробелами/юникодом во всех действиях
  (сейчас `Process.run` без shell — ок, но стоит покрыть тестом).

## Функциональность UI

- [ ] **P2. Поиск/фильтр** репозиториев по имени и быстрые фильтры
  («только с обновлениями», «только с ошибками», «скрыть заброшенные»).
- [ ] **P2. Пользовательская сортировка** в интерфейсе (имя / размер / коммиты /
  дата) — сейчас порядок жёстко задан в `groupProjects`.
- [ ] **P2. Заголовки столбцов** в построчном виде (Коммиты · Размер · Посл. pull) —
  сейчас смысл столбцов раскрывается только в tooltip (`_MetricCell`).
- [ ] **P2. Прогресс для пакетных Pull/Push.** Прогресс-бар есть только у скана
  (`_runOnProjects` его не показывает) — добавить счётчик/полосу и для операций.
- [ ] **P2. Обновить один репозиторий** без полного пере-скана (кнопка refresh
  в строке; логика уже есть в `GitScanner.inspectRepo`).
- [ ] **P3. Персист свёрнутых групп.** `_ProjectListAreaState._collapsed` живёт
  только в памяти — сохранять в `SharedPreferences`.
- [ ] **P3. Экспорт/сохранение лога** (`_LogPanel`) в файл.
- [x] **P1. Переход в источник** по ссылке origin.
- [x] **P1. Удаление** репозитория с диска — пункт меню «Удалить с диска…»
  с подтверждением; папка перемещается в Корзину (обратимо).
- [x] **P1. Добавление репозитория** — кнопка в шапке клонирует репозиторий
  по git-URL в выбранную директорию сканирования и сразу добавляет его в список.
- [ ] **P1. Запуск обновления по cron** (`_LogPanel`) в файл.

## Git-логика

- [ ] **P1. Обработка diverged при pull.** `pullDefaultBranch` использует
  `--ff-only`; при разошедшихся ветках падает с ошибкой. Предлагать выбор
  rebase/merge или явно сообщать причину и не помечать красным «ошибкой».
- [ ] **P2. Выбор ветки** помимо `main`/`master` (кастомная default-ветка на
  репозиторий; надёжнее определять через `remote HEAD`).
- [ ] **P2. Инвалидация `targetRemote` при смене настроек.** Кэш хранит
  `targetRemote`, вычисленный по прежнему `remoteHost`; при смене хоста нужно
  пересчитать (сейчас берётся из кэша, пока TTL не истёк).
- [ ] **P3. Таймауты/повторы** для сетевых git-операций (fetch/pull/push).
- [ ] **P3. Проверка секретов** в репозитории.

## Расписание и уведомления

- [ ] **P2. Нативное уведомление** когда плановый скан нашёл обновления
  (сейчас только строка в логе).
- [ ] **P3. Расписание переживает закрытие приложения** — `Timer.periodic`
  работает только при открытом окне; для фонового запуска нужен launchd-агент.

## Инфраструктура и качество

- [ ] **P1. Не коммитить `project.pbxproj` с отключённой подписью.**
  `scripts/macos_nosign.sh` — локальный шаг сборки; его правки не должны попадать
  в репозиторий. Восстанавливать файл после сборки или собирать во временной копии.
- [ ] **P1. Подпись и нотаризация DMG.** Сейчас релиз unsigned → Gatekeeper
  предупреждает при запуске. Добавить codesign + notarize (или инструкцию).
- [ ] **P2. CI (GitHub Actions):** `flutter analyze` + `flutter test` +
  сборка macOS + артефакты DMG/zip на тег.
- [ ] **P2. Расширить тесты:** `Format` (bytes/relativeDate), кэш `RepoCache`
  (round-trip JSON), TTL-логика `GitScanner`, widget-тесты списка/групп.
- [ ] **P2. Обновить `pubspec.yaml` description** — сейчас «push на GitLab»,
  а приёмник уже generic.
- [x] **P3. Локализация (i18n).** Строки вынесены в ARB (`lib/l10n/`), поддержка
  RU/EN с переключателем языка в шапке и настройках, выбор сохраняется.
- [ ] **P3. README/скриншоты** — добавить скриншоты обоих видов и тёмной темы.
